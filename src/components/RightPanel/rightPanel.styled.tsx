import {Container} from "@mui/material";
import {styled} from "@mui/material/styles";

export const StyledRightPanel = styled(Container)`
    display: flex;
    flex-direction: column;
    flex: .5;
    margin: 15px;

`